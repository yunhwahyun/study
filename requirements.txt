# 파이썬 파일로 설치
# [bash] pip install -r requirements.txt
# [bash Mac] pip3 install -r requirements.txt
# package_name==version 형식으로 작성. version 생략 시 최신 버전 설치.
# pyinstaller --onefile OOO.py              # PyInstaller로 .exe 만들기 (Windows 기준)
# pyinstaller --onefile --windowed OOO.py   # pygame 테트리스인 경우
# pyinstaller --onefile --add-data "img/;img/" OOO.py       # 리소스 파일 포함. 경로는 윈도우: "폴더;폴더", 맥/리눅스는 "폴더:폴더"
pygame
pyinstaller